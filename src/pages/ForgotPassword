import { emailService } from "../api/EmailService.ts";

const handleSubmit = async () => {
  try {
    await emailService.sendVerificationCode(email);
    alert("Check your email for the reset link");
  } catch (err: any) {
    alert(err.message);
  }
};
